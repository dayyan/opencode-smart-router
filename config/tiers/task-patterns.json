{
  "taskPatterns": {
    "fast": [
      "search",
      "grep",
      "read",
      "git-info",
      "ls",
      "lookup-docs/types",
      "count",
      "exists-check",
      "rename"
    ],
    "light": [
      "simple-edit",
      "small-fix",
      "config-tweak"
    ],
    "medium": [
      "impl-feature",
      "refactor",
      "write-tests",
      "bugfix(≤2)",
      "edit-logic",
      "code-review",
      "build-fix",
      "create-file",
      "db-migrate",
      "api-endpoint",
      "config-update"
    ],
    "focused": [
      "deep-debug",
      "single-system-review",
      "complex-bugfix"
    ],
    "heavy": [
      "arch-design",
      "debug(≥3fail)",
      "sec-audit",
      "perf-opt",
      "migrate-strategy",
      "multi-system-integration",
      "tradeoff-analysis",
      "rca"
    ]
  },
  "modes": {
    "normal": {
      "defaultTier": "medium",
      "description": "Balanced quality and cost — delegates based on task complexity"
    },
    "budget": {
      "defaultTier": "fast",
      "description": "Aggressive cost savings — defaults to cheapest tier, escalates only when needed",
      "overrideRules": [
        "default→@fast unless edits/complex-reasoning needed",
        "@medium ONLY: multi-file-edit/refactor/test-suite/build-fix",
        "@heavy ONLY: user-requested OR ≥2 @medium failures",
        "trivial(grep/read/glob)→direct,no-delegate",
        "batch related searches→single @fast",
        "uncertain @fast vs @medium→@fast,escalate on fail",
        "dispatch caps (tighter): always include CAP:5 when dispatching @fast, CAP:2 for @medium, CAP:2 for @heavy",
        "orchestrator self-cap: ≤1 direct read-only call per turn; 2nd need → dispatch @fast"
      ]
    },
    "quality": {
      "defaultTier": "focused",
      "description": "Quality-first — uses stronger models more liberally for better results",
      "overrideRules": [
        "default→@focused incl single-system analysis when deep-context matters",
        "@heavy: arch/debug/security/multi-system-coord",
        "@medium: standard implementation and multi-file changes",
        "@fast ONLY: trivial single-tool ops (1 grep/1 read)",
        "prefer thoroughness over speed",
        "dispatch caps: always include CAP:none in dispatch prompts — subagents may explore as needed for deep context",
        "subagents still run redundancy check; CAP:none only lifts the numeric cap, not the stop-on-redundancy rule"
      ]
    },
    "deep": {
      "defaultTier": "heavy",
      "description": "Deep analysis mode — prioritizes thorough architecture/debug work with long heavy runs",
      "overrideRules": [
        "default→@medium for implementation and multi-file changes",
        "@heavy for architecture/debug/security/tradeoff-analysis by default",
        "allow long heavy runs before fallback; avoid premature downshift",
        "trivial(grep/read/glob)→direct,no-delegate",
        "if task is composite: explore@fast then execute@heavy",
        "dispatch caps: CAP:none for @heavy only; @fast=CAP:8 and @medium=CAP:5 baselines apply",
        "subagents still run redundancy check; CAP:none only lifts the numeric cap"
      ]
    }
  },
  "fallback": {
    "global": {
      "anthropic": [
        "openai",
        "google",
        "github-copilot"
      ],
      "openai": [
        "anthropic",
        "google",
        "github-copilot"
      ],
      "github-copilot": [
        "anthropic",
        "openai",
        "google"
      ],
      "google": [
        "openai",
        "anthropic",
        "github-copilot"
      ],
      "opencode-go": [
        "openai",
        "google",
        "github-copilot"
      ]
    }
  },
  "rules": [
    "[tier:X] tag in plan → delegate to X",
    "plan:fast/cheap→@fast | plan:light→@light | plan:medium→@medium | plan:focused→@focused | plan:heavy→@heavy",
    "default preference: read-only work → @fast; localized/simple implementation → @light; standard implementation → @medium; deep single-system → @focused; architecture/multi-system → @heavy",
    "orchestrate=self, execute=subagent (info-gathering IS execution, not orchestration)",
    "trivial (≤1 tool call, no expected follow-up) → direct, skip-delegate",
    "orchestrator self-cap (TARGET): ≤2 direct read-only calls per turn; 3rd need → dispatch @fast (exceed only with 1-line reason)",
    "dispatch caps baseline: @fast=CAP:8, @light=CAP:7, @medium=CAP:5, @focused=CAP:4, @heavy=CAP:3 (omit directive = baseline; include CAP:N to override; CAP:none disables)",
    "before dispatching @heavy: gather context first (usually via @fast); if context is already sufficient, dispatch directly",
    "if self is opus: skip-@heavy (do locally); still prefer routing broader read-only exploration to @fast",
    "min(cost, adequate-tier)"
  ]
}
